To remove all items stowed do the following

Go to where the dotfiles are stored and run the stow command with -D

if the stow command that was run to setup the linked files was
    stow config

then to remove the stowed files run this command
    stow -D config


